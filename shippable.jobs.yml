jobs:
  - name: j1
    type: runSh
    steps:
      - TASK:
        - script: echo "Hello from j1"
        - script: echo versionName=$(date) > $JOB_DIR/my_state.env
        - script: echo foo="bar" >> $JOB_DIR/my_state.env
      - OUT: my_state
